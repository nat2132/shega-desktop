# Plan: Find and Translate Untranslated Text

## Goal
Identify all untranslated (hardcoded) strings in the reports, reminders, audit logs, contacts, orders, debts, expense/budget, and sidebar sections, then add them to the translation system.

## Approach

### Step 1: Compare `en` vs `am` keys in translations.ts
- Read the full Amharic (`am`) section of `src/renderer/src/i18n/translations.ts` (starts at line 2323)
- Compare every key in the `en` section against the `am` section
- Identify missing Amharic keys for these sections: `reports`, `reminders`, `audit_logs`, `contacts`, `orders`, `debt`, `expense`, `budgets`, `tabs`
- Add the missing Amharic translations

### Step 2: Scan page components for hardcoded strings
Read these page files and find any user-visible text NOT wrapped in `t()`:
- `src/renderer/src/pages/Reports.tsx`
- `src/renderer/src/pages/ReminderHistory.tsx`
- `src/renderer/src/pages/AuditLogs.tsx`
- `src/renderer/src/pages/Contacts.tsx`
- `src/renderer/src/pages/Orders.tsx`
- `src/renderer/src/pages/OrderDetail.tsx`
- `src/renderer/src/pages/DebtManagement.tsx`
- `src/renderer/src/pages/Expenses.tsx`
- `src/renderer/src/pages/BudgetManagement.tsx`
- `src/renderer/src/components/app-sidebar.tsx` (already verified — all using t())
- `src/renderer/src/components/nav-user.tsx` (user menu in sidebar footer)

### Step 3: Add missing translation keys
For each hardcoded string found:
1. Add the English key to the `en` object in `translations.ts`
2. Add the Amharic translation to the `am` object
3. Replace the hardcoded string in the component with `t('key')`

### Step 4: Verify
- Run `npm run typecheck` or `npm run build` to ensure no TypeScript errors
- Check that all new keys appear in both `en` and `am` sections

## Files to Modify
- `src/renderer/src/i18n/translations.ts` — add missing keys in both en/am sections
- Page components with hardcoded strings (determined in Step 2)

## Verification
- Build the app and visually inspect each tab in Amharic mode
- Search for remaining hardcoded strings: `grep` for quoted strings not wrapped in `t()`
